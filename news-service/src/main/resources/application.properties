# Quarkus configuration
quarkus.application.name=news-service
quarkus.http.port=8083

# News configuration
news.api.key=${NEWS_API_KEY:}
news.symbols=${NEWS_SYMBOLS:BTC,ETH,BNB,SOL,XRP}
news.poll.interval=${NEWS_POLL_INTERVAL:5m}
news.max.headlines=${NEWS_MAX_HEADLINES:5}

# Redis configuration
redis.host=${REDIS_HOST:localhost}
redis.port=${REDIS_PORT:6379}
quarkus.redis.hosts=${QUARKUS_REDIS_HOSTS:redis://localhost:6379}

# Logging
quarkus.log.level=INFO
quarkus.log.category."cl.ioio.finbot".level=DEBUG
quarkus.log.console.format=%d{HH:mm:ss} %-5p [%c{2.}] (%t) %s%e%n
