# Quarkus configuration
quarkus.application.name=ingestion-service
quarkus.http.port=8081

# Binance API configuration (Crypto - 24/7)
binance.symbols=${BINANCE_SYMBOLS:btc,eth,bnb,sol,xrp}

# Redis configuration
redis.host=${REDIS_HOST:localhost}
redis.port=${REDIS_PORT:6379}
quarkus.redis.hosts=${QUARKUS_REDIS_HOSTS:redis://localhost:6379}

# Java 21 Virtual Threads (Ultra Performance)
quarkus.virtual-threads.enabled=true
quarkus.thread-pool.virtual-threads=true

# Redis Performance Tuning
quarkus.redis.max-pool-size=50
quarkus.redis.max-pool-waiting=1000
quarkus.redis.timeout=10s

# HTTP/IO Performance
quarkus.http.io-threads=8
quarkus.http.worker-threads=200

# Logging
quarkus.log.level=INFO
quarkus.log.category."cl.ioio.finbot".level=DEBUG
quarkus.log.console.format=%d{HH:mm:ss} %-5p [%c{2.}] (%t) %s%e%n
